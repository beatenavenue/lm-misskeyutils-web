{
  "name": "lm-misskeyutils-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "express": "node dev/dev-server.js",
    "tunnel": "dotenv -- sh -c 'cloudflared tunnel run --token \"$CLOUDFLARE_TOKEN\"'",
    "dev": "npm-run-all --parallel express tunnel"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "express": "^4.19.2",
    "npm-run-all": "^4.1.5"
  }
}
